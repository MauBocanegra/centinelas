import 'package:centinelas_app/application/core/strings.dart';
import 'package:flutter/material.dart';

class UserListItemReportErrorView extends StatelessWidget {
  const UserListItemReportErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(reportErrorString),
    );
  }
}
