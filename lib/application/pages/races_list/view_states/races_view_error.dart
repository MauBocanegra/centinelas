import 'package:flutter/material.dart';

class RacesViewError extends StatelessWidget {
  const RacesViewError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('ERROR! Intenta de nuevo, por favor'),
    );
  }
}
