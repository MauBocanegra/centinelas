import 'package:flutter/material.dart';

class RaceItemProvider extends StatelessWidget {
  const RaceItemProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
